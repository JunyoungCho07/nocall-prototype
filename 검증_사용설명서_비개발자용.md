# NoCall 모델 검증 — 사용설명서 (비개발자용)

> 개발 지식 없이도 따라 할 수 있게 만든 설명서입니다.
> 목표: 받은 AI 모델이 5개 제품(콜라·물·시리얼·종이컵·컵라면)을 잘 인식하는지 **확인**하는 것.
> Windows 컴퓨터 + 웹캠 기준. 처음 1회 설치 약 15분, 이후 더블클릭만 하면 됩니다.

---

## 0. 무엇을 하게 되나요?

1. 컴퓨터에 프로그램 2개를 **설치** (처음 1번만)
2. 받은 **프로젝트 폴더**를 컴퓨터에 둡니다
3. 폴더 안의 **`2_데모실행.bat` 더블클릭** → 웹캠으로 제품을 비추면 화면에 이름이 뜹니다

설치만 넘기면 나머지는 더블클릭이 전부입니다.

---

## 1. 프로그램 2개 설치 (처음 1번만)

### (1) uv 설치  — AI 실행에 필요한 도구
1. 화면 왼쪽 아래 **돋보기(검색)** 클릭 → `powershell` 입력
2. **Windows PowerShell** 을 마우스 **오른쪽 클릭 → "관리자 권한으로 실행"**
3. 파란 창이 뜨면 아래 줄을 **그대로 복사해서 붙여넣고 Enter**:
   ```
   powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
   ```
4. 설치가 끝나면 그 창을 닫습니다.

### (2) (폴더를 git으로 받는 경우만) Git 설치
> 개발자가 폴더를 직접 보내줬다면 **이 단계는 건너뛰세요.**
1. https://git-scm.com/download/win 접속 → 자동 다운로드되는 설치 파일 실행
2. 설치 중 옵션은 **계속 "Next"** 만 눌러 끝까지 진행 (기본값 그대로면 됩니다)

---

## 2. 프로젝트 폴더 준비

### 방법 A (가장 쉬움 — 추천): 개발자가 보내준 폴더 사용
1. 개발자가 보내준 **`nocall-prototype` 폴더**(압축이면 압축 풀기)를
   `바탕화면` 같은 찾기 쉬운 곳에 둡니다.
2. 끝! 바로 3번으로 가세요.

### 방법 B: 직접 내려받기 (Git 설치한 경우)
1. 폴더를 둘 곳에서 빈 공간 **오른쪽 클릭 → "터미널에서 열기"**(또는 PowerShell)
2. 아래를 **한 줄씩** 복사·붙여넣기 (각 줄 끝에 Enter):
   ```
   git lfs install
   git clone https://github.com/JunyoungCho07/nocall-prototype.git
   ```
   - 로그인 화면이 뜨면 GitHub 계정으로 로그인하세요(접근 권한 필요).

---

## 3. 실행하기 (더블클릭)

폴더 `nocall-prototype` 를 열면 아래 두 파일이 보입니다:

| 파일 | 하는 일 |
|---|---|
| **`1_모델검증.bat`** | 웹캠 없이, 모델이 제품을 잘 맞히는지 **숫자로** 확인 |
| **`2_데모실행.bat`** | 웹캠으로 **실시간** 인식 데모 (진짜 확인) |

### ① 숫자 검증 — `1_모델검증.bat` 더블클릭
- 검은 창이 뜨고 잠시 후 표가 나옵니다.
- **5개 항목(cola·water·cereal-box·paper-cup·cup-noodle)이 모두 `PASS`** 면 정상입니다.
- (파란 경고 글씨가 나와도 정상입니다. 빨간 에러만 문제예요.)

> ⚠️ 처음 실행 시 "Windows의 PC 보호" 같은 파란 경고가 뜨면
> **"추가 정보" → "실행"** 을 누르세요. (출처가 확실한 파일이라 안전합니다.)

### ② 실제 데모 — `2_데모실행.bat` 더블클릭
- 잠시 후 **인터넷 창이 자동으로 열립니다.**
- 웹캠 사용을 **허용**하세요.
- 제품을 카메라에 비추면 네모 상자와 **제품 이름 + 정확도(%)** 가 표시됩니다.

---

## 4. 화면 보는 법 (데모)

```
┌─────────────────────────────┬──────────────┐
│   [웹캠 영상]               │  장바구니     │
│                             │  ───────────  │
│   ┌───────┐ ┌───────┐       │  콜라   2  3600│
│   │ cola  │ │ cola  │ ← 이름·정확도 │  물     1  1000│
│   │  92%  │ │  90%  │       │  ───────────  │
│   └───────┘ └───────┘       │  합계   4600  │
│   (계산대 위에 한 겹으로 펼침) │ [결제 QR] [초기화]│
└─────────────────────────────┴──────────────┘
```

- **계산대 위에 제품을 서로 겹치지 않게 한 겹으로 펼쳐** 놓으면 → 카메라에 보이는 개수만큼 오른쪽 장바구니에 **자동 집계**됩니다
- 제품을 **빼면** 장바구니에서도 **빠집니다** (약 1초 뒤 반영 — 깜빡임 방지용 평활화)
- 카메라는 **위에서 내려다보는 각도**가 가장 정확합니다 (옆에서 보면 앞 물건이 뒷 물건을 가림)

---

## 5. "잘 됐다" 판단 기준 (체크리스트)

- [ ] `1_모델검증.bat` 표에서 5개 모두 **PASS**
- [ ] 데모에서 5개 제품 모두 **이름이 맞고 정확도 70% 이상**
- [ ] 한 겹으로 펼친 제품 개수가 장바구니에 맞게 집계된다 (예: 콜라 2개 → 콜라 2)
- [ ] 엉뚱한 물건을 제품으로 잘못 잡는 일이 심하지 않다

이 4개가 되면 모델 검증 완료입니다. 👍

---

## 6. 잘 안 될 때

| 이런 증상 | 이렇게 하세요 |
|---|---|
| 검은 창에 빨간 글씨 `uv ... not recognized` | uv 설치(1단계)가 안 된 것. PowerShell 다시 열고 1-(1) 재실행 |
| 영상이 까만 화면 | 다른 앱(줌·카메라)이 웹캠을 쓰는 중. 그 앱들을 끄고 다시 더블클릭 |
| 인터넷 창이 안 열림 | 직접 주소창에 `localhost:8000` 입력 |
| 제품 인식이 약함/이름이 틀림 | 더 밝은 곳에서, 제품을 카메라에 더 가까이·정면으로. 그래도 약하면 개발자에게 알려주세요 |
| "Windows가 PC 보호" 경고 | "추가 정보" → "실행" 클릭 |
| 모델 파일이 깨졌다는 메시지 | (방법 B로 받은 경우) PowerShell에서 `git lfs pull` 입력. 또는 개발자에게 폴더를 다시 받기 |

---

## 7. 막히면

화면(검은 창의 빨간 글씨 또는 인터넷 화면)을 **캡처해서 개발자에게 보내주세요.**
어디서 멈췄는지 알면 바로 도와드릴 수 있습니다.

---

### 개발자 메모 (폴더 전달 시)
방법 A로 보낼 때는 `.venv`, `train/data`(용량 큼)는 빼고,
**`models/nocall.pt`(실제 21MB 파일)** 가 포함됐는지 꼭 확인해서 보내세요.
(git LFS 포인터 몇 줄짜리가 아니라 진짜 파일이어야 합니다.)
